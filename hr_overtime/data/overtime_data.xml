<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <!-- Compute overtime button in action menu -->
        <record id="model_hr_attendance_action_compute_overtime" model="ir.actions.server">
            <field name="name">Calcular horas extras</field>
            <field name="model_id" ref="hr_overtime.model_hr_attendance"/>
            <field name="binding_model_id" ref="hr_overtime.model_hr_attendance"/>
            <field name="state">code</field>
            <field name="code">action = records.action_compute_overtime()</field>
        </record>
</odoo>
